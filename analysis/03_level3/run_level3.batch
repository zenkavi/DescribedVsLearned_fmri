#!/bin/bash

#SBATCH -J level3-{MODELNUM}-{MODELNAME}-{REGNAME}-{SIGN}-tfce-{TFCE}
#SBATCH -c 8

# Outputs ----------------------------------
#SBATCH -o /shared/.out/level3-{MODELNUM}-{MODELNAME}-{SIGN}-{REGNAME}-tfce-{TFCE}.out
#SBATCH -e /shared/.err/level3-{MODELNUM}-{MODELNAME}-{SIGN}-{REGNAME}-tfce-{TFCE}.err
# ------------------------------------------

export DATA_PATH=/shared/bids_nifti_wface/derivatives/nilearn/glm/level2/{MODELNUM}_reg-rt{REG_RT}
export OUT_PATH=/shared/bids_nifti_wface/derivatives/nilearn/glm/level3/{MODELNUM}_reg-rt{REG_RT}/{MODELNAME}
export CODE_PATH=/shared/code/analysis/03_level3
export BM_PATH=/shared/bids_nifti_wface/derivatives/fmriprep

aws s3 sync s3://described-vs-experienced/bids_nifti_wface/derivatives/nilearn/glm/level2/{MODELNUM}_reg-rt{REG_RT} $DATA_PATH/
aws s3 sync s3://described-vs-experienced/bids_nifti_wface/derivatives/fmriprep/ $BM_PATH/ --exclude "*" --include "*brain_mask.nii.gz"

docker run --rm -e DATA_PATH=/data -e OUT_PATH=/out -e BM_PATH=/bm \
-v $DATA_PATH:/data -v $OUT_PATH:/out -v $BM_PATH:/bm -v $CODE_PATH:/code \
zenkavi/fsl:6.0.3 ./code/level3.py --mnum {MODELNUM} --mname {MODELNAME} -r {REGNAME} --reg_rt {REG_RT} -s {SIGN} -tf {TFCE}

aws s3 sync $OUT_PATH/{REGNAME} s3://described-vs-experienced/bids_nifti_wface/derivatives/nilearn/glm/level3/{MODELNUM}/{MODELNAME}/{REGNAME}
